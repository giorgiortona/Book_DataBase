# Book_DataBase
Microsoft Access database designed to manage a bookstore. Book data was imported from a CSV dataset and processed using SQL queries, with basic normalization applied to separate publishers and structure the data.
